include_queue = "true"
timeout = 30
memory_size = 128
zip_path="send-flow-response/send-flow-response.zip"
handler = "index.handler"
environmentVars = {
  "ENVIRONMENT" = "${ENVIRONMENT}",
  "DOMAIN" = "${DOMAIN}"
}
