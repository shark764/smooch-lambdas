include_queue = "true"
timeout = 30
memory_size = 128
zip_path="update-interaction-metadata/update-interaction-metadata.zip"
handler = "index.handler"
environmentVars = {
  "ENVIRONMENT" = "${ENVIRONMENT}",
  "DOMAIN" = "${DOMAIN}"
}
