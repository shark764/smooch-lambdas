timeout = 30
memory_size = 128
zip_path="update-smooch-web-integration/update-smooch-web-integration.zip"
handler = "index.handler"
runtime = "nodejs12.x"
environmentVars = {
  "ENVIRONMENT" = "${ENVIRONMENT}"
  "DOMAIN" = "${DOMAIN}"
}