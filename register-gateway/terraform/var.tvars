include_queue = "true"
timeout = 30
memory_size = 128
zip_path="register-gateway/register-gateway.zip"
handler = "index.handler"
environmentVars = {
  "ENVIRONMENT" = "${ENVIRONMENT}",
  "DOMAIN" = "${DOMAIN}",
  "ACCOUNT_ID" = "${ACCOUNT_ID}"
}
