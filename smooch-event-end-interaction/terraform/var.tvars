include_queue = "true"
timeout = 30
memory_size = 128
zip_path="smooch-event-end-interaction/smooch-event-end-interaction.zip"
handler = "index.handler"
environmentVars = {
  "ENVIRONMENT" = "${ENVIRONMENT}",
  "DOMAIN" = "${DOMAIN}"
}
