include_queue = "true"
timeout = 30
memory_size = 128
zip_path="smooch-action-disconnect/smooch-action-disconnect.zip"
handler = "index.handler"
environmentVars = {
  "ENVIRONMENT" = "${ENVIRONMENT}",
  "DOMAIN" = "${DOMAIN}"
}
