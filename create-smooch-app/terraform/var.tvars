timeout = 30
memory_size = 128
zip_path="create-smooch-app/create-smooch-app.zip"
handler = "index.handler"
runtime = "nodejs12.x"
environmentVars = {
  "ENVIRONMENT" = "${ENVIRONMENT}"
  "DOMAIN" = "${DOMAIN}"
}