import packageInfo from '../../package.json';

export function buildTestHeaders(authHeaders) {
    return {
        ...authHeaders,
        'User-Agent': 'smooch-core-js/' + packageInfo.version,
    };
}
