include_queue = "true"
timeout = 30
memory_size = 128
zip_path="upload-artifact-file/upload-artifact-file.zip"
handler = "index.handler"
environmentVars = {
  "ENVIRONMENT" = "${ENVIRONMENT}",
  "DOMAIN" = "${DOMAIN}"
}
