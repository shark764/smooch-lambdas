timeout = 30
memory_size = 128
zip_path="delete-smooch-web-integration/delete-smooch-web-integration.zip"
handler = "index.handler"
environmentVars = {
  "ENVIRONMENT" = "${ENVIRONMENT}"
  "DOMAIN" = "${DOMAIN}"
}