timeout = 30
memory_size = 128
zip_path="send-message/send-message.zip"
handler = "index.handler"
environmentVars = {
  "ENVIRONMENT" = "${ENVIRONMENT}",
  "DOMAIN" = "${DOMAIN}",
  "SNS_REPORTING_ARN" = "arn:aws:sns:${REGION}:${ACCOUNT_ID}:${REGION}-${ENVIRONMENT}-agent-message"
}