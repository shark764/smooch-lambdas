{
  "name": "smooch-lambdas",
  "version": "1.12.0",
  "dependencies": {},
  "scripts": {
    "build": "node install.js && node zip.js",
    "zip": "node zip.js",
    "lint": "eslint . --fix",
    "test": "eslint . && jest",
    "postinstall": "node install.js",
    "precommit": "npm test",
    "prepush": "npm test"
  },
  "jest": {
      "reporters": [
        "<rootDir>/utils/no-logs-reporter.js",
        "<rootDir>/utils/summary-reporter.js"
    ],
    "verbose": false
  },
  "author": "SerenovaLLC",
  "license": "UNLICENSED",
  "devDependencies": {
    "eslint": "^6.8.0",
    "eslint-config-airbnb-base": "^14.0.0",
    "eslint-plugin-import": "^2.18.2",
    "husky": "0.14.3",
    "jest": "^24.9.0"
  }
}
