custom_queue_mapping = "smooch-webhook"
timeout = 30
memory_size = 512
zip_path="smooch-webhook/smooch-webhook.zip"
handler = "index.handler"
runtime = "nodejs12.x"
environmentVars = {
  "ENVIRONMENT" = "${ENVIRONMENT}",
  "DOMAIN" = "${DOMAIN}"
}