include_queue = "true"
queue_delay_seconds = 30
timeout = 30
memory_size = 128
zip_path="smooch-action-add-participant/smooch-action-add-participant.zip"
handler = "index.handler"
environmentVars = {
  "ENVIRONMENT" = "${ENVIRONMENT}",
  "DOMAIN" = "${DOMAIN}"
}
