include_queue = "true"
timeout = 30
memory_size = 128
zip_path="smooch-action-collect-message-response/smooch-action-collect-message-response.zip"
handler = "index.handler"
environmentVars = {
  "ENVIRONMENT" = "${ENVIRONMENT}",
  "DOMAIN" = "${DOMAIN}"
}
