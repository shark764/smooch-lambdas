timeout = 30
memory_size = 128
zip_path="update-whatsapp-integration/update-whatsapp-integration.zip"
handler = "index.handler"
runtime = "nodejs12.x"
environmentVars = {
  "ENVIRONMENT" = "${ENVIRONMENT}",
  "DOMAIN" = "${DOMAIN}"
}