include_queue = "true"
timeout = 30
memory_size = 128
zip_path="send-reporting-event/send-reporting-event.zip"
handler = "index.handler"
runtime = "nodejs12.x"
environmentVars = {
  "ENVIRONMENT" = "${ENVIRONMENT}",
  "DOMAIN" = "${DOMAIN}"
  "ACCOUNT_ID" = "${ACCOUNT_ID}"
}
