include_queue = "true"
timeout = 30
memory_size = 128
zip_path="send-reporting-event/send-reporting-event.zip"
handler = "index.handler"
environmentVars = {
  "ENVIRONMENT" = "${ENVIRONMENT}",
  "DOMAIN" = "${DOMAIN}"
}
