timeout = 30
memory_size = 128
zip_path="send-conversation-event/send-conversation-event.zip"
handler = "index.handler"
environmentVars = {
  "ENVIRONMENT" = "${ENVIRONMENT}",
  "DOMAIN" = "${DOMAIN}"
}